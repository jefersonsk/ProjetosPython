else:
                            if filial_atual is not None:
                                codigo_livro = int(cache[0])
                                preco = float(cache[1])
                                quantidade = int(cache[2])
